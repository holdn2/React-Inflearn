import { useSearchParams } from "react-router-dom";

const Home = () => {
  //   const [params, setParams] = useSearchParams(); 쿼리 스트링 받아오기
  //   console.log(params.get("value"));

  return <div>Home</div>;
};

export default Home;
